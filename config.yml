title: Primeiro teste
description: Cobaia
template:
  repo: primeiro-teste-template
  name: primeiro-teste
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
